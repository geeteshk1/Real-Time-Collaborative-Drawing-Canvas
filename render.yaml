services:
  - type: web
    name: collaborative-canvas
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: WEBSOCKET_PATH
        value: /ws
      - key: ALLOWED_ORIGINS
        value: "*"
    healthCheckPath: /
